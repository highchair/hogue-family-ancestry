# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Hogue Family Geneology
description: 
github_username: jhogue
github_repo:

baseurl: "" # the subpath of your site, e.g. /blog
port: 6040
url: "http://127.0.0.1:6040" # the base hostname & protocol for your site, e.g. http://example.com
prod_url: "https://hogue-family-tree.highchairdesign.com"
destination: _site/

# Build settings
markdown: kramdown
highlighter: rouge
kramdown:
  input: GFM
  syntax_highlighter: rouge


# permalink: /:categories/:year/:month/:day/:title:output_ext
permalink: /:categories/:title/
relative_permalinks: false
image_path: "assets/people/"

collections:
  people:
    output: true
    permalink: /person/:slug/

defaults:
  - scope:
      path: ""
      type: "people"
    values:
      layout: person

sass:
  sass_dir: _sass
  style: compressed


# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - .git/
  - .gitignore
  - .htaccess
  - .nova/
  - LICENSE
  - readme.md
  - sources
